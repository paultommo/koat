{{ 'section-koat-hero.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

<div class="koat-hero color-{{ section.settings.color_scheme }} gradient section-{{ section.id }}-padding">
  <div class="koat-hero__inner page-width">
    <div>
      {%- assign badge_blocks = section.blocks | where: 'type', 'badge' -%}
      {%- if badge_blocks.size > 0 -%}
        <div class="koat-hero__badges">
          {%- for block in badge_blocks -%}
            <span class="koat-badge koat-badge--{{ block.settings.tone }}" {{ block.shopify_attributes }}>
              {{ block.settings.text | escape }}
            </span>
          {%- endfor -%}
        </div>
      {%- endif -%}

      {%- if section.settings.heading != blank -%}
        <h1 class="koat-hero__heading">{{ section.settings.heading }}</h1>
      {%- endif -%}

      {%- if section.settings.body != blank -%}
        <div class="koat-hero__body rte">{{ section.settings.body }}</div>
      {%- endif -%}

      {%- assign button_block = section.blocks | where: 'type', 'buttons' | first -%}
      {%- if button_block -%}
        <div class="koat-hero__buttons" {{ button_block.shopify_attributes }}>
          {%- if button_block.settings.button_label_1 != blank -%}
            <a
              {% if button_block.settings.button_link_1 == blank %}
                role="link" aria-disabled="true"
              {% else %}
                href="{{ button_block.settings.button_link_1 }}"
              {% endif %}
              class="button{% if button_block.settings.button_style_secondary_1 %} button--secondary{% else %} button--primary{% endif %}"
            >
              {{- button_block.settings.button_label_1 | escape -}}
            </a>
          {%- endif -%}
          {%- if button_block.settings.button_label_2 != blank -%}
            <a
              {% if button_block.settings.button_link_2 == blank %}
                role="link" aria-disabled="true"
              {% else %}
                href="{{ button_block.settings.button_link_2 }}"
              {% endif %}
              class="button{% if button_block.settings.button_style_secondary_2 %} button--secondary{% else %} button--primary{% endif %}"
            >
              {{- button_block.settings.button_label_2 | escape -}}
            </a>
          {%- endif -%}
        </div>
      {%- endif -%}

      {%- assign reviews_block = section.blocks | where: 'type', 'google_reviews' | first -%}
      {%- if reviews_block -%}
        <a
          href="{{ reviews_block.settings.review_url | default: 'https://www.google.com/search?q=koat+reviews' }}"
          class="koat-hero__reviews"
          target="_blank"
          rel="noopener"
          {{ reviews_block.shopify_attributes }}
        >
          <svg class="koat-hero__reviews-logo" viewBox="0 0 48 48" aria-hidden="true">
            <path fill="#FFC107" d="M43.6 20.5H42V20.5H24v7h11.3C33.7 31.9 29.3 35 24 35c-6.6 0-12-5.4-12-12s5.4-12 12-12c3.1 0 5.9 1.2 8.1 3.1l5-5C33.6 5.6 29 3.5 24 3.5 12.7 3.5 3.5 12.7 3.5 24S12.7 44.5 24 44.5 44.5 35.3 44.5 24c0-1.2-.1-2.4-.3-3.5z"/>
            <path fill="#FF3D00" d="M6.3 14.7l5.8 4.2C13.7 15.2 18.5 12 24 12c3.1 0 5.9 1.2 8.1 3.1l5-5C33.6 6.5 29 4.5 24 4.5c-7.5 0-14 4.3-17.7 10.2z"/>
            <path fill="#4CAF50" d="M24 44.5c5 0 9.6-1.9 13-5l-6-5.1c-2 1.4-4.6 2.1-7 2.1-5.3 0-9.7-3.1-11.3-7.6l-5.9 4.5c3.5 6.9 10.1 11.1 17.2 11.1z"/>
            <path fill="#1976D2" d="M43.6 20.5H42V20.5H24v7h11.3c-.8 2.2-2.2 4.1-4.1 5.4l6 5.1C40.6 35.8 44.5 30.4 44.5 24c0-1.2-.1-2.4-.3-3.5z"/>
          </svg>
          <span class="koat-hero__reviews-stars" aria-hidden="true">★★★★★</span>
          <span class="koat-hero__reviews-rating">{{ reviews_block.settings.rating }}</span>
          <span class="koat-hero__reviews-count">{{ reviews_block.settings.review_count }} reviews on Google</span>
        </a>
      {%- endif -%}
    </div>

    <div class="koat-hero__media">
      {%- assign tag_block = section.blocks | where: 'type', 'tag' | first -%}
      {%- if tag_block and tag_block.settings.text != blank -%}
        <div class="koat-hero__tag" {{ tag_block.shopify_attributes }}>
          <span class="koat-tag koat-tag--{{ tag_block.settings.tone }}">{{ tag_block.settings.text | escape }}</span>
        </div>
      {%- endif -%}
      {%- if section.settings.image != blank and section.settings.image.aspect_ratio -%}
        {{
          section.settings.image
          | image_url: width: 1000
          | image_tag: widths: '300, 450, 600, 750, 900, 1000', sizes: '(min-width: 750px) 46rem, 80vw'
        }}
      {%- else -%}
        <img
          src="{{ 'KOAT-blueberries.png' | asset_url }}"
          alt="KOAT blueberry kefir oat bar"
          width="1000"
          height="1000"
          loading="lazy"
        >
      {%- endif -%}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "KOAT Hero",
  "tag": "section",
  "class": "section",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-5"
    },
    {
      "type": "inline_richtext",
      "id": "heading",
      "label": "Heading",
      "default": "snacks your gut loves"
    },
    {
      "type": "richtext",
      "id": "body",
      "label": "Body text",
      "default": "<p>Protein-rich oats meet probiotic-packed kefir. Real fruit, real cultures, genuinely delicious.</p>"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Product image"
    },
    {
      "type": "header",
      "content": "Padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Top padding",
      "default": 64
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Bottom padding",
      "default": 64
    }
  ],
  "blocks": [
    {
      "type": "badge",
      "name": "Badge",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Text",
          "default": "12g Protein"
        },
        {
          "type": "select",
          "id": "tone",
          "label": "Tone",
          "options": [
            { "value": "ink", "label": "Ink outline" },
            { "value": "solid", "label": "Solid ink" },
            { "value": "blue", "label": "Blue" },
            { "value": "banana", "label": "Banana" },
            { "value": "blueberry", "label": "Blueberry" },
            { "value": "raspberry", "label": "Raspberry" },
            { "value": "leaf", "label": "Leaf" }
          ],
          "default": "banana"
        }
      ]
    },
    {
      "type": "buttons",
      "name": "Buttons",
      "limit": 1,
      "settings": [
        {
          "type": "header",
          "content": "Button 1"
        },
        {
          "type": "text",
          "id": "button_label_1",
          "label": "Label",
          "default": "Shop the bars"
        },
        {
          "type": "url",
          "id": "button_link_1",
          "label": "Link"
        },
        {
          "type": "checkbox",
          "id": "button_style_secondary_1",
          "label": "Use secondary style",
          "default": false
        },
        {
          "type": "header",
          "content": "Button 2"
        },
        {
          "type": "text",
          "id": "button_label_2",
          "label": "Label",
          "default": "Why kefir?"
        },
        {
          "type": "url",
          "id": "button_link_2",
          "label": "Link"
        },
        {
          "type": "checkbox",
          "id": "button_style_secondary_2",
          "label": "Use secondary style",
          "default": true
        }
      ]
    },
    {
      "type": "google_reviews",
      "name": "Google reviews",
      "limit": 1,
      "settings": [
        {
          "type": "text",
          "id": "rating",
          "label": "Rating",
          "default": "4.9"
        },
        {
          "type": "text",
          "id": "review_count",
          "label": "Review count",
          "default": "220+"
        },
        {
          "type": "url",
          "id": "review_url",
          "label": "Link to Google reviews"
        }
      ]
    },
    {
      "type": "tag",
      "name": "Corner tag",
      "limit": 1,
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Text",
          "default": "new drop"
        },
        {
          "type": "select",
          "id": "tone",
          "label": "Tone",
          "options": [
            { "value": "blue", "label": "Blue" },
            { "value": "ink", "label": "Ink" },
            { "value": "banana", "label": "Banana" },
            { "value": "raspberry", "label": "Raspberry" },
            { "value": "leaf", "label": "Leaf" }
          ],
          "default": "banana"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "KOAT Hero",
      "blocks": [
        { "type": "badge", "settings": { "text": "12g Protein", "tone": "banana" } },
        { "type": "badge", "settings": { "text": "Live Kefir Cultures", "tone": "solid" } },
        { "type": "buttons" },
        { "type": "google_reviews" },
        { "type": "tag" }
      ]
    }
  ]
}
{% endschema %}
